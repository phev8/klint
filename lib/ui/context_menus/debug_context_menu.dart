import 'package:flutter/material.dart';

class DebugContextMenu extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      width: 200,
      height: 300,
      color: Colors.blue,
    );
  }
}
